<view class="card" bind:tap="toMusiclist">
  <image class="card-img" src="{{cardInfo.picUrl}}"></image>
  <view class="card-name">{{cardInfo.name}}</view>
  <text class="card-count">{{realNum}}</text>
</view>